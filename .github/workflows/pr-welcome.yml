name: PR Welcome

on:
  pull_request_target:
    types: [opened, edited, reopened]

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions-cool/pr-welcome@v0.0.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          refuse-issue-label: 'bug'
          need-creator-authority: 'write'
          comment: |
            HI
          close: true
